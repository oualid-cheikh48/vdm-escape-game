PORT=3000

PGHOST=localhost
PGPORT=5432
PGDATABASE=vdm_escape_game
PGUSER=postgres
PGPASSWORD=postgres

# Secret de signature des JWT. À changer en production (chaîne aléatoire longue,
# ex: openssl rand -hex 32). Ne jamais committer une vraie valeur.
JWT_SECRET=change_moi_en_production
JWT_EXPIRES_IN=2h
